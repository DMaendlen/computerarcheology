#! /bin/bash

# Aufgabe 3: rekursive Dateisuchfunktion

function suche()
{
  cd $1 || return
  for i in * ; do
    if( [ -e $2 ] && [ $2 = $i ] ); then
      echo "$1/$2 exists";
    elif ( [ -d $i ] ); then
      suche "$1/$i" $2;
    fi
  done
}
